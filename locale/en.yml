michaelbelgium-ai-autoreply:
  admin:
    settings:
      platform_label: Platform
      platform_help: "Select the AI platform to use."
      api_key_label: API Key
      api_key_help: Get the API key from <a>{platform}</a>.
      model_label: Model
      model_help: "Learn more about <a>{platform} models</a> (default: <code>{model}</code>).."
      max_tokens_label: Max Tokens
      max_tokens_help: <a>What are tokens and how to count them?</a>
      temperature_label: Temperature
      temperature_help: Controls the randomness. The range is usually between 0 and 2 but some platforms have other ranges.
      user_prompt_label: User Prompt
      user_prompt_help: Enter the user id that will be used to generate responses.
      user_prompt_badge_label: User Prompt Badge
      user_prompt_badge_help: Text that will be displayed next to the user prompt.
      enable_on_discussion_started_label: Enable on discussion start
      enable_on_discussion_started_help: When enabled, AI replies only at discussion start. When disabled, the discussion becomes a chat between the OP and the assistant; the assistant replies to every OP post.
      enabled_tags_label: Enabled Tags
      enabled_tags_help: Select which tags will be used to generate the answer.
    permissions:
      use_chatgpt_assistant_label: Use AI assistant